#include <stdio.h>

void main()


{
    int list_a[5] = {5, 4, 7, 2, 1};

    
    for (int i = 0; i < 5; i++) {
    //printf("%d %d %d %d %d\n", list_a[0], list_a[1], list_a[2], list_a[3], list_a[4]);
        int min_num = i;
        for (int j = i + 1; j < 5; j++) {
            if (list_a[min_num] > list_a[j]) {
                int temp = list_a[j];
                list_a[j] = list_a[min_num];
                list_a[min_num] = temp;
            }
        }
        

        
    }
    printf("최종 : %d %d %d %d %d\n", list_a[0], list_a[1], list_a[2], list_a[3], list_a[4]);

} 